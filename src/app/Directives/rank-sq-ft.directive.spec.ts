import { RankSqFtDirective } from './rank-sq-ft.directive';

describe('RankSqFtDirective', () => {
  it('should create an instance', () => {
    const directive = new RankSqFtDirective();
    expect(directive).toBeTruthy();
  });
});
