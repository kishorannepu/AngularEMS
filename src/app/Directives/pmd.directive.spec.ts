import { PmdDirective } from './pmd.directive';

describe('PmdDirective', () => {
  it('should create an instance', () => {
    const directive = new PmdDirective();
    expect(directive).toBeTruthy();
  });
});
